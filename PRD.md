# 轻案证析产品需求文档（PRD）

> 面向基层检察院的帮信罪事实—证据要素化与量刑情节核验智能体

## 0. 文档信息

- 文档版本：V1.0
- 产品阶段：比赛原型 / 受控试点 MVP
- 依据文档：《轻案证析融合方案》
- 目标读者：产品、设计、前端、后端、算法、测试、法律业务专家、安全与项目管理人员
- 首版技术建议：Streamlit + Python + SQLite；后续可迁移至 React/Vue + FastAPI + 生产级数据库
- 本文约定：“材料支持某事实”仅表示材料中存在对应记载，不表示系统评价证据能力、证明力或证明标准

## 1. 产品概述

### 1.1 产品定位

“轻案证析”是帮信罪案件的单案审查工作台。系统将案卷材料转换为可回到原文的结构化事实、实体、待证要素和证据关系，提示客观材料缺口与内部冲突，并在人工确认后提供规则可复算的量刑情节核验和可追溯的类案差异参考。

产品采用“受控 Plan 工作流 + 确定性 Python 工具 + 版本化法律知识库 + DeepSeek V4 Flash + 人工复核”的模式。Plan 是任务控制器，不是可跳过安全步骤的开放式代理。

### 1.2 核心问题

1. 多类型案卷中的主体、账户、设备、时间和金额信息分散，人工摘录耗时且易漏。
2. 材料中的客观记载、陈述者评价和系统无法确认的信息容易混淆。
3. 待证要素、事实命题和证据来源之间缺少直观、可回溯的映射。
4. 同一主体、账号、日期和金额在不同材料中可能不一致。
5. 量刑情节、规范版本和类案差异需要重复查找与核对。
6. 补充材料后，传统工作方式需要重复整理大量不受影响的内容。
7. 通用大模型可能补造事实、错误引用或越权输出定罪、证明标准和确定刑期结论。

### 1.3 产品目标

- 形成“上传材料—脱敏确认—受控执行—人工复核—校核导出”的完整闭环。
- 每个结构化事实均可定位到文档版本、页码和原文片段。
- 将事实、材料中的评价和未确认信息分别保存和展示。
- 自动组织帮信罪四类审查维度，集中显示支持材料、反向材料、缺口与冲突。
- 新增、替换、删除材料后只重算受影响成果，保留未受影响的人工确认。
- 所有量刑计算均由确定性规则执行，并显示输入、公式、规则版本和中间步骤。
- 所有模型调用、人工修改、规则计算和报告导出均可审计、可复现。

### 1.4 非目标与责任边界

首版明确不做：

- 不认定犯罪嫌疑人是否构成帮信罪；
- 不判断证据是否达到“确实、充分”的证明标准；
- 不对证据能力、真实性和证明力自动评分；
- 不输出确定刑期或最终量刑建议；
- 不自动生成起诉书或办案决定；
- 不将公开裁判文书样本等同于全部司法案件；
- 不进行地区犯罪预测或特定人群风险画像；
- 不支持帮信罪之外的罪名；
- 不对音视频自动转写；
- 不在未经机构授权、未完成脱敏确认时向外部模型发送真实案卷；
- 不使用模型隐性推理作为业务依据或向用户展示隐性推理。

## 2. 用户与权限

### 2.1 用户角色

#### 承办检察官

- 创建、归档和重新开启案件任务；
- 查看案件全部材料和结构化成果；
- 确认、修正、排除事实与实体关系；
- 处理关键缺口和冲突；
- 主动开启量刑核验和类案检索；
- 批准正式报告导出。

#### 检察官助理

- 上传、分类、替换材料；
- 处理 OCR 和脱敏异常；
- 初步复核事实、实体、映射、缺口和冲突；
- 提交关键结果给承办检察官确认；
- 不得独立批准正式报告或发布法律规则。

#### 辅助办案人员

- 上传材料、维护目录、修正低风险解析结果；
- 默认无权查看脱敏映射、启动量刑工具或导出完整原文。

#### 业务管理员

- 维护帮信罪要素模板、文书类型、缺口规则、法律规则和类案数据；
- 规则按“草稿—复核—发布—停用”管理；
- 默认无权读取真实案件正文。

#### 安全审计员

- 查看登录、权限、外部调用、人工修改、导出和规则发布日志；
- 无权修改案件业务数据。

#### 系统管理员

- 管理用户、组织、服务配置、存储和运行状态；
- 默认无权读取案卷正文；临时授权必须有期限、理由和审计记录。

### 2.2 权限原则

- 访问由“组织 + 用户角色 + 案件成员关系 + 具体操作权限”共同决定。
- 跨部门、跨组织访问默认拒绝。
- 查看原始材料、查看脱敏映射、允许外部调用、启动量刑、导出报告分别授权。
- 已归档案件只读；重新开启需承办检察官授权。
- 权限校验必须在服务端完成，界面隐藏按钮不能替代权限控制。

## 3. 范围与发布分期

### 3.1 P0：核心审查闭环

- 登录、任务列表、案件创建和成员权限；
- PDF、DOCX、TXT、PNG/JPG 上传、解析、OCR、去重和版本管理；
- 本地脱敏、逐项复核和外部调用确认；
- 请求安全门控；
- INITIAL_PLAN、计划确认、执行、暂停、恢复和终止；
- 事实与评价分离、实体提取和原文绑定；
- 帮信罪四类要素卡及事实—证据映射；
- 人工复核、修改理由和影响预览；
- 缺口与冲突发现；
- 时间轴和基础实体关系视图；
- REPLAN、成果失效、增量更新和版本比较；
- 输出校核、基础报告和审计日志导出。

### 3.2 P1：受控辅助能力

- 量刑情节核验和可复算规则区间；
- 结构化特征 + 语义相似度类案检索；
- 类案相同点、不同点及限制说明；
- 工作流完成后的只读依据问答；
- 法律规则和类案库管理；
- 系统质效和类案覆盖看板。

### 3.3 P2：试点增强

- 复杂版面及银行流水表格复核；
- 分析情景与多版本比较；
- 细粒度 RBAC、统一身份认证和完整安全加固；
- 金标准管理、回归评测和发布质量门禁；
- 正式前后端分离架构。

## 4. 核心业务概念

- 案件任务：一次围绕单个案件开展的材料审查工作，可包含多名涉案人员。
- 文档：用户视角的一份材料；替换文件产生新的文档版本而非覆盖旧文件。
- 片段：可定位的最小原文单位，由文档版本、页码、块号、字符区间和原文哈希确定。
- 实体：人物、组织、银行账户、支付账户、银行卡、手机号、设备、SIM 卡、IP、地点等。
- 客观事实：材料中可定位的主体、行为、时间、金额等具体记载。
- 材料评价：侦查人员、犯罪嫌疑人、证人或文书制作人员作出的判断性表达。
- 未确认信息：材料不足、表达模糊或模型不能可靠归类的信息。
- 待证要素：按帮信罪专属模板组织的审查维度，不代表系统作出实体判断。
- 证据关系：某片段对某事实的“记载、反向记载、提及或评价来源”关系。
- 缺口：按法律要素规则、材料目录规则或经验核对规则发现的待补充、待定位事项。
- 冲突：不同材料或人工确认结果在主体、时间、账号、金额、行为等方面不一致。
- 成果：事实集、实体集、映射表、时间轴、缺口清单、计算结果或报告等版本化输出。
- Plan：由白名单动作构成的有向无环执行计划。
- 人工复核：对模型或规则产出的确认、修正、排除、暂不判断或重新开启。
- 分析情景：不覆盖正式案件事实的参数分支，用于比较不同情节采用方式。

## 5. 信息架构与页面

### 5.1 一级页面

1. 登录与任务列表
2. 创建案件
3. 单案工作台
4. 材料中心
5. 脱敏确认
6. Plan 确认与运行
7. 事实与实体复核
8. 要素—证据映射
9. 缺口与冲突
10. 时间轴与关系图
11. 量刑情节工具
12. 类案检索
13. 报告与导出
14. 依据解读问答
15. 管理后台
16. 质效看板

### 5.2 单案工作台固定信息

- 案件编号、任务名称、承办人、适用地区、案件材料类型；
- 当前案件状态、Plan 版本和最近检查点；
- 材料总数、解析失败数、脱敏待确认数；
- 已提取事实数、待复核数、缺口数、冲突数；
- 四类要素的材料发现和人工确认状态；
- 最近变更、待办和阻塞原因；
- 量刑、类案、报告的可用条件；
- 产品边界提示。

## 6. 主流程与交互逻辑

### 6.1 首次审查主流程

1. 用户创建案件，填写案件编号、承办部门、适用地区、行为时间范围和材料属性。
2. 用户批量上传材料；系统计算哈希、识别格式、解析或 OCR，并生成页级片段。
3. 系统展示材料状态；失败文件可单独重试或标记“不纳入分析”。
4. 本地脱敏工具识别敏感信息，用户在原文/脱敏文本对照页逐项复核。
5. 高风险敏感项全部确认后，用户才能允许本次文档版本参与外部模型调用。
6. 安全门控判断用户请求为允许、需澄清或拒绝。
7. Plan 读取案件状态并生成 INITIAL_PLAN，展示强制步骤、可选步骤、依赖、预计调用次数和缺失材料。
8. 用户确认计划；可关闭量刑、类案等可选项，不可关闭脱敏、证据绑定和输出校核。
9. 执行器按依赖逐项执行。每一步先检查前置条件，后检查 Schema、溯源、必填字段和越界风险。
10. 事实、评价、实体和要素映射进入复核工作台。
11. 用户确认、修正、排除或暂不判断；关键修改先展示下游影响。
12. 系统生成缺口、冲突、时间轴和关系图；高风险项进入人工复核。
13. 用户可主动运行量刑工具或类案检索。
14. 输出校核通过后生成报告；正式报告由有权限用户导出。
15. 必做项完成、关键复核结束且校核通过后，Plan 状态为 `completed`，开放只读依据问答。

### 6.2 新增、替换或删除材料

1. 用户选择操作类型并上传新文件或选择待删除版本。
2. 系统在不破坏当前成果的情况下完成哈希、解析和脱敏。
3. 系统比较文档、片段和依赖图，生成影响预览：
   - 新增、修改、删除了哪些片段；
   - 影响哪些事实、实体、要素、缺口、冲突、时间轴、计算和报告；
   - 哪些已确认成果可继续复用。
4. 用户确认影响预览后，系统创建新 Plan 版本。
5. 受影响成果标记为 `stale` 或 `pending_review`；未受影响且已校验成果保持 `valid`。
6. 执行器从最早受影响步骤开始处理。
7. 系统展示新旧版本差异并要求复核关键变化。
8. `stale` 成果不得进入正式报告或量刑计算。

### 6.3 人工纠错

1. 用户在事实、实体、映射、缺口或冲突上点击“修正”。
2. 界面显示当前值、原文、来源版本、最近确认人和可能影响范围。
3. 用户填写新值和必填修改理由。
4. 系统进行类型、格式、权限和版本冲突校验。
5. 用户二次确认后，系统创建新版本并保留旧值。
6. 依赖图将相关下游成果标记为待更新，生成 REPLAN。
7. 若用户取消，则不改变业务数据、不生成重算任务。

### 6.4 暂停、恢复和失败

- 暂停只在当前原子步骤安全结束后生效，并保存检查点。
- 恢复时只执行未完成或已失效步骤。
- 模型超时、限流或 Schema 失败默认最多自动重试一次。
- 超过步骤数、模型调用数或费用上限时，Plan 必须停止并显示原因。
- 单文件失败不得中止其他文件的本地解析。
- 无法修复时状态为 `waiting_user`、`blocked` 或 `failed`，不得无限循环。

## 7. 页面级功能与详细交互

### 7.1 任务列表与创建案件

任务列表支持“我的案件、协作案件、待复核、等待用户、运行中、已完成、已归档”视图，可按部门、承办人、状态和时间筛选。

创建案件字段：

- 任务名称：必填，1—100 字；
- 案件编号：必填，同组织重复时提示但允许创建分案；
- 承办部门、承办人：必填；
- 适用地区：必填；未选择时量刑工具禁用；
- 涉案人员：至少一名，可后续补充；
- 行为时间范围：可暂缺，但缺失时规则计算禁用；
- 材料属性：合成材料 / 脱敏真实材料 / 授权真实材料；
- 外部模型策略：禁止 / 脱敏确认后允许；
- 成员及权限：创建人默认案件管理员。

创建成功后进入工作台并写入审计事件。用户点击删除时执行归档，不进行物理删除。

### 7.2 材料中心

每份材料展示：

- 逻辑名称、文件名、文书类型、文件大小和页数；
- SHA-256、版本号、上传人和上传时间；
- 解析状态、OCR 状态、脱敏状态和是否纳入分析；
- 失败原因、被替代版本和下游依赖数量。

交互规则：

- 支持拖拽批量上传；
- 同哈希文件提示重复，不创建新版本；
- “替换”创建新版本并保留旧版本；
- “删除/不纳入”前展示下游影响；
- 人工可修正文书类型，修改需留痕；
- 页级预览支持原图和 OCR 文本对照；
- 加密 PDF 提示用户提供可读取版本，不尝试绕过保护；
- 损坏文件隔离处理，不阻塞其他文件。

### 7.3 脱敏确认

页面采用左侧原文、右侧脱敏结果和底部敏感项列表。

- 支持姓名、身份证号、手机号、地址、账号、案件编号等类型筛选；
- 每项显示检测器、置信度、位置和替换标记；
- 支持补充遮蔽、撤销误遮蔽、批量应用同类规则；
- 原始值只在有“查看脱敏映射”权限时短时显示；
- 高风险项未确认时“允许外部调用”按钮禁用；
- 脱敏确认绑定文档版本，文件替换后自动失效；
- 脱敏映射独立加密保存，不进入模型载荷和普通日志。

### 7.4 Plan 确认与运行

计划确认页展示：

- 系统对用户意图的理解；
- 已有材料和缺失材料；
- 强制步骤、可选步骤及依赖关系；
- 每一步的输入摘要、预计输出、工具类型和人工节点；
- 将被复用、失效或重新计算的成果；
- 预计耗时、模型调用次数和预算。

用户可以：

- 确认计划；
- 关闭可选步骤；
- 补充说明或材料；
- 暂停、取消或重试失败项；
- 展开查看每一步输入摘要、结构化输出、校验结果和错误。

用户不能：

- 关闭安全门控、脱敏、原文绑定或输出校核；
- 修改 Plan 为白名单外动作；
- 在依赖未满足时强制启动下游步骤；
- 查看模型隐性推理。

### 7.5 事实与实体复核

采用三栏布局：

- 左栏：事实、材料评价、未确认信息和低置信度队列；
- 中栏：结构化字段、实体及关系；
- 右栏：原文页码、上下文、文档版本和定位。

事实操作：

- 确认、修正、排除、暂不判断、拆分、合并；
- 修正和排除必须填写理由；
- “排除”仅表示不纳入当前分析，不表示材料无效；
- 数值字段使用十进制定点类型，展示单位和计算来源；
- 模型置信度只用于复核排序，不代表事实真伪。

实体操作：

- 合并同一主体别名；
- 拆分误合并实体；
- 绑定人物与账户、设备、手机号；
- 修改主体身份、控制关系、核心账号时先展示影响范围。

### 7.6 要素—证据映射

按四个维度展示：

1. 客观帮助行为；
2. 主观明知相关事实；
3. 情节严重相关事实；
4. 程序与量刑情节。

每项要素展示：

- 当前状态；
- 支持材料、反向材料、中性提及和仅有评价的材料；
- 事实列表及人工确认状态；
- 冲突、缺口和待人工判断问题；
- 要素模板版本和映射依据。

允许状态：

- 未审查；
- 已发现材料；
- 未发现对应材料；
- 存在冲突；
- 待人工确认；
- 已人工确认。

禁止使用“已证明”“不成立”“证据不足”“构成犯罪”等实体判断词。

### 7.7 缺口与冲突

缺口类型：

- 无对应材料；
- 仅有结论性表述；
- 引用附件未上传；
- 缺少主体、时间、金额或计算来源；
- 主观评价缺少具体事实对应；
- 量刑情节缺少证明材料；
- 规范适用信息不足。

冲突类型：

- 主体、时间、金额、账号、行为、陈述冲突；
- 新旧材料冲突；
- 材料与人工确认结果冲突。

处置动作：

- 标记待补材料；
- 指派处理人；
- 关联已有材料；
- 标记为表述差异；
- 暂不处理；
- 关闭并填写理由；
- 重新开启。

严重级别只表示复核优先级，不表示实体风险。关闭记录不物理删除，补充材料后可自动转为“已消除”并保留历史。

### 7.8 时间轴与关系图

- 时间轴包含开户、交易、提供账户、冻结、讯问、退赃、程序节点等事件；
- 时间必须带精度：准确日期、月份、区间或未知；
- 多来源同一事件并列展示；
- 日期冲突不自动选择“正确值”；
- 拖动只改变展示顺序，修改业务日期需进入事实修正流程；
- 关系图展示人物、账户、设备、手机号及资金关系；
- 每个节点和边均显示来源数量、确认状态并可跳转原文；
- 无来源关系不得显示为已确认。

### 7.9 量刑情节工具

工具必须由有权限用户主动开启，分四步：

1. 选择适用地区和行为时间；
2. 选择已确认事实和拟采用情节；
3. 核验材料、规则版本和适用条件；
4. 展示计算步骤、参考区间和风险提示。

计算结果必须显示：

- 规则标题、发布机关、文号、条款、适用地区和有效期；
- 每个参数的值、单位、来源事实或人工输入；
- 公式、中间值、舍入方式和结果区间；
- 未采用情节和拒绝计算原因；
- 与基准情景的差异；
- “规范化计算参考，非最终量刑建议”声明。

以下情况拒绝计算：

- 地区不匹配；
- 行为时间不明确或规则有效期不匹配；
- 规则冲突或未完成专业复核；
- 关键参数存在未处理冲突；
- 使用了 `stale` 或未经确认的事实。

### 7.10 类案检索

检索流程：

1. 用户选择地区、年份、审级、行为方式、金额、账户数、获利、退赃和认罪认罚等条件；
2. 系统先执行罪名、地区、年份、案件类型硬过滤；
3. 再按结构化特征和中文语义相似度排序；
4. 返回 3—5 个候选；
5. 模型只基于当前案件已确认成果和候选原文生成相同点、不同点；
6. 用户决定是否纳入报告。

每个候选必须展示案号、法院、裁判日期、来源、获取日期、原文定位、相似特征、关键差异和样本限制。不得以案例平均刑期推导当前案件刑期。

### 7.11 报告与导出

正式导出前运行：

- 原文反向定位校验；
- 法条存在性、版本和案件时间匹配校验；
- 金额、日期和计算一致性校验；
- 每项要素来源校验；
- 反向材料遗漏校验；
- 越界表述校验；
- 待复核、`stale` 和阻塞项校验。

可导出：

- Word/PDF 审查辅助报告；
- Excel 要素—证据映射表；
- 量刑情节核验表；
- 类案差异报告；
- 运行日志和人工修改记录。

正式报告包含生成时间、案件数据版本、Plan 版本、规则版本、未解决缺口和冲突、能力边界、水印、导出人和导出编号。校核失败时禁止正式导出，可允许带醒目标记的草稿。

### 7.12 依据解读问答

- 仅在 Plan 为 `completed` 时开放；
- 回答只使用本案已确认成果、原文和已核验知识库；
- 关键回答必须提供来源跳转；
- 无依据时答复“当前材料中未发现”；
- 问答默认只读；
- 用户要求修改事实、补材料、替换文件或重新计算时，弹出影响提示并创建 REPLAN，不直接写库。

## 8. 状态机

### 8.1 案件状态

`DRAFT` → `INGESTING` → `REDACTION_PENDING` → `PLAN_PENDING` → `RUNNING` → `REVIEW_REQUIRED` → `COMPLETED`

旁路状态：

- `WAITING_USER`：等待材料、确认或纠正；
- `BLOCKED`：权限、数据、规则或系统条件阻断；
- `PAUSED`：用户主动暂停；
- `FAILED`：达到重试上限且无法修复；
- `CANCELLED`：取消本轮计划；
- `ARCHIVED`：案件归档，只读。

### 8.2 Plan 模式

- `INITIAL_PLAN`：首次材料审查；
- `REPLAN`：材料或人工结果变化后的影响重算；
- `RESUME_PLAN`：从检查点恢复；
- `POST_WORKFLOW_QA`：完成后的只读解释。

### 8.3 Plan 项状态

- `PENDING`
- `RUNNING`
- `COMPLETED`
- `NEEDS_REVIEW`
- `WAITING_USER`
- `STALE`
- `BLOCKED`
- `FAILED`
- `SKIPPED`（仅可选步骤）
- `CANCELLED`

### 8.4 成果有效性

- `VALID`：当前输入和规则版本下有效；
- `PENDING_REVIEW`：已生成但需要人工复核；
- `STALE`：上游变化，必须重算或复核；
- `INVALID`：校验失败，不得用于下游。

### 8.5 完成条件

Plan 只有同时满足以下条件才能为 `completed`：

- 所有必做项为 `completed` 或 `approved`；
- 所有阻塞级冲突已处理；
- 关键事实和量刑输入完成规定级别的人工确认；
- 不存在进入正式成果的 `stale` 或 `invalid` 数据；
- 输出校核通过；
- 未超过预算且无未处理高风险安全事件。

## 9. Plan 白名单动作

允许动作：

`PARSE_DOCUMENTS`、`RUN_LOCAL_REDACTION`、`CLASSIFY_REQUEST`、`ASK_CLARIFICATION`、`EXTRACT_FACTS`、`EXTRACT_ENTITIES`、`MAP_ELEMENTS`、`BIND_EVIDENCE`、`CHECK_GAPS`、`CHECK_CONFLICTS`、`BUILD_TIMELINE`、`BUILD_RELATION_GRAPH`、`REQUEST_HUMAN_REVIEW`、`RUN_SENTENCING_CHECK`、`RUN_CASE_RETRIEVAL`、`VERIFY_OUTPUT`、`GENERATE_REPORT`、`ADD_DOCUMENT`、`REPLACE_DOCUMENT`、`REMOVE_DOCUMENT`、`CORRECT_FACT`、`CORRECT_ENTITY`、`CREATE_SCENARIO`、`COMPARE_VERSIONS`、`EXPLAIN_RESULT`、`PAUSE_PLAN`、`CANCEL_PLAN`。

动作校验器必须拒绝白名单外动作。Plan 不能直接写业务表，所有修改均由执行器在权限、Schema、版本和依赖校验通过后提交。

## 10. 数据结构

### 10.1 通用约定

- 主键使用不可预测的 UUID/ULID；
- 时间按 UTC 存储，界面按机构时区展示；
- 金额使用 `Decimal`，同时保存币种和单位，禁止浮点运算；
- 业务表包含 `created_at`、`created_by`、`updated_at`、`version`；
- 人工修正和文档替换采用追加版本，不覆盖历史；
- JSON 扩展字段需有版本化 Schema；
- 原始敏感值不得进入普通日志；
- 所有正式成果保存输入快照哈希和生成器版本。

### 10.2 组织与权限

#### `organizations`

- `org_id`
- `name`
- `org_type`
- `region_code`
- `status`

#### `users`

- `user_id`
- `org_id`
- `display_name`
- `role_codes`
- `status`
- `last_login_at`

#### `case_memberships`

- `case_id`
- `user_id`
- `case_role`
- `permissions_json`
- `granted_by`
- `granted_at`
- `expires_at`

### 10.3 案件

#### `case_tasks`

- `case_id`
- `case_no`
- `task_name`
- `org_id`
- `department_id`
- `lead_user_id`
- `applicable_region`
- `conduct_start_date`
- `conduct_end_date`
- `material_class`：`SYNTHETIC` / `REDACTED_REAL` / `AUTHORIZED_REAL`
- `external_model_policy`：`DENY` / `ALLOW_AFTER_REDACTION`
- `status`
- `current_plan_id`
- `version`
- `archived_at`

#### `case_subjects`

- `case_subject_id`
- `case_id`
- `person_entity_id`
- `role_in_case`
- `is_primary`

### 10.4 文档、片段与脱敏

#### `documents`

- `document_id`
- `case_id`
- `logical_name`
- `document_type`
- `current_version_id`
- `include_in_analysis`
- `created_by`

#### `document_versions`

- `document_version_id`
- `document_id`
- `version_no`
- `file_name`
- `mime_type`
- `file_size`
- `sha256`
- `page_count`
- `storage_uri`
- `parse_status`
- `ocr_status`
- `redaction_status`
- `supersedes_version_id`
- `uploaded_at`

#### `chunks`

- `chunk_id`
- `document_version_id`
- `page_no`
- `block_no`
- `char_start`
- `char_end`
- `raw_text_hash`
- `redacted_text`
- `bbox_json`
- `ocr_confidence`

#### `redaction_items`

- `redaction_id`
- `document_version_id`
- `pii_type`
- `original_value_encrypted`
- `replacement_token`
- `locator_json`
- `detector`
- `confidence`
- `review_status`
- `reviewer_id`
- `reviewed_at`

文书类型至少包括：起诉意见书、讯问笔录、证人证言、银行流水、电子数据、证据目录、扣押清单、冻结材料、具结书、退赃材料、其他。

### 10.5 实体与关系

#### `entities`

- `entity_id`
- `case_id`
- `entity_type`
- `canonical_name`
- `masked_display_name`
- `attributes_json`
- `origin`：`MODEL` / `RULE` / `HUMAN`
- `confidence`
- `review_status`
- `version`

实体类型：`PERSON`、`ORGANIZATION`、`BANK_ACCOUNT`、`PAYMENT_ACCOUNT`、`BANK_CARD`、`PHONE_NUMBER`、`DEVICE`、`SIM_CARD`、`IP_ADDRESS`、`LOCATION`。

#### `entity_aliases`

- `alias_id`
- `entity_id`
- `alias_text`
- `source_chunk_id`

#### `entity_relations`

- `relation_id`
- `from_entity_id`
- `to_entity_id`
- `relation_type`
- `start_at`
- `end_at`
- `source_chunk_id`
- `confidence`
- `review_status`

关系类型：`OWNS`、`USES`、`CONTROLS`、`PROVIDES_TO`、`TRANSFERS_TO`、`INTRODUCES`、`BINDS`、`CONTACTS`、`UNKNOWN_RELATION`。

### 10.6 事实、评价与证据

#### `facts`

- `fact_id`
- `case_id`
- `case_subject_id`（可空）
- `fact_type`：`OBJECTIVE_FACT` / `MATERIAL_OPINION` / `UNCONFIRMED_INFORMATION`
- `predicate_code`
- `subject_entity_id`
- `object_entity_id`
- `normalized_value`
- `unit`
- `time_start`
- `time_end`
- `time_precision`
- `fact_text`
- `speaker_entity_id`（评价主体）
- `confidence`
- `review_status`
- `origin`
- `version`
- `supersedes_fact_id`

复核状态：`UNREVIEWED`、`CONFIRMED`、`CORRECTED`、`EXCLUDED`、`DEFERRED`。

#### `fact_evidence_links`

- `link_id`
- `fact_id`
- `chunk_id`
- `relation_type`：`SUPPORTS` / `CONTRADICTS` / `MENTIONS` / `SOURCE_OF_OPINION`
- `quote`
- `quote_hash`
- `page_no`
- `locator_json`
- `created_by_type`

`SUPPORTS` 仅表示材料记载与事实命题方向一致，不得解释为系统完成证明力评价。

### 10.7 待证要素

#### `element_definitions`

- `element_definition_id`
- `code`
- `name`
- `dimension`
- `description`
- `required_fact_types_json`
- `gap_rules_json`
- `version`
- `effective_from`
- `effective_to`
- `status`

维度：`OBJECTIVE_ASSISTANCE`、`SUBJECTIVE_KNOWLEDGE_RELATED`、`SERIOUS_CIRCUMSTANCE_RELATED`、`PROCEDURE_AND_SENTENCING`。

#### `element_assessments`

- `assessment_id`
- `case_id`
- `case_subject_id`（可空）
- `element_definition_id`
- `status`
- `summary`
- `human_review_required`
- `review_status`
- `artifact_version`

状态：`NOT_REVIEWED`、`MATERIALS_FOUND`、`NO_MATERIAL_FOUND`、`CONFLICTING_MATERIALS`、`INSUFFICIENT_METADATA`、`PENDING_HUMAN_REVIEW`。

#### `element_fact_links`

- `assessment_id`
- `fact_id`
- `link_role`：`SUPPORTING` / `COUNTER` / `CONTEXTUAL`

### 10.8 缺口与冲突

#### `gaps`

- `gap_id`
- `case_id`
- `element_assessment_id`
- `gap_type`
- `description`
- `expected_material_type`
- `severity`
- `status`
- `rule_id`
- `rule_category`：`LEGAL_ELEMENT` / `MATERIAL_COMPLETENESS` / `PRACTICE_CHECK`
- `resolution`
- `resolved_by`
- `resolved_at`

#### `conflicts`

- `conflict_id`
- `case_id`
- `conflict_type`
- `left_fact_id`
- `right_fact_id`
- `description`
- `severity`
- `status`
- `resolution_type`
- `resolution_reason`
- `resolved_by`
- `resolved_at`

严重级别：`LOW`、`MEDIUM`、`HIGH`、`BLOCKING`。

### 10.9 Plan、成果与检查点

#### `plans`

- `plan_id`
- `case_id`
- `plan_version`
- `mode`
- `user_intent`
- `safety_status`
- `status`
- `parent_plan_id`
- `completion_criteria_json`
- `max_steps`
- `max_model_calls`
- `budget_limit`
- `created_at`

#### `plan_items`

- `item_id`
- `plan_id`
- `action`
- `status`
- `input_refs_json`
- `output_refs_json`
- `requires_human_confirmation`
- `retry_count`
- `retry_limit`
- `started_at`
- `completed_at`
- `error_code`

#### `plan_item_dependencies`

- `item_id`
- `depends_on_item_id`
- `dependency_type`

#### `artifacts`

- `artifact_id`
- `case_id`
- `artifact_type`
- `version`
- `validity_status`
- `content_hash`
- `generated_by_run_id`
- `input_snapshot_hash`

#### `artifact_dependencies`

- `upstream_type`
- `upstream_id`
- `downstream_artifact_id`
- `dependency_reason`

#### `checkpoints`

- `checkpoint_id`
- `plan_id`
- `last_completed_item_id`
- `state_snapshot_uri`
- `created_at`

### 10.10 人工复核

#### `human_reviews`

- `review_id`
- `case_id`
- `target_type`
- `target_id`
- `decision`：`CONFIRM` / `CORRECT` / `EXCLUDE` / `DEFER` / `REOPEN`
- `before_value_json`
- `after_value_json`
- `reason`
- `reviewer_id`
- `reviewed_at`
- `impact_analysis_id`

### 10.11 法律规则与量刑

#### `legal_rules`

- `rule_id`
- `title`
- `issuer`
- `document_no`
- `article_no`
- `text`
- `jurisdiction`
- `published_at`
- `effective_from`
- `effective_to`
- `validity_status`
- `source_url`
- `verified_by`
- `verified_at`
- `version`

#### `sentencing_scenarios`

- `scenario_id`
- `case_id`
- `case_subject_id`
- `name`
- `base_scenario_id`
- `region`
- `conduct_date`
- `status`
- `created_by`

#### `sentencing_factors`

- `factor_id`
- `scenario_id`
- `factor_type`
- `value`
- `unit`
- `adoption_status`
- `source_fact_id`
- `source_material_status`

#### `calculation_runs`

- `calculation_id`
- `scenario_id`
- `rule_version_set_json`
- `input_snapshot_json`
- `result_lower`
- `result_upper`
- `unit`
- `status`
- `refusal_reason`

#### `calculation_steps`

- `step_id`
- `calculation_id`
- `sequence`
- `rule_id`
- `formula`
- `input_values_json`
- `output_value`
- `rounding_rule`

### 10.12 类案、报告、模型与审计

#### `similar_cases`

- `similar_case_id`
- `case_number`
- `court`
- `region`
- `judgment_date`
- `structured_features_json`
- `full_text_uri`
- `source`
- `license_info`
- `acquired_at`
- `dataset_version`

#### `retrieval_runs`

- `retrieval_run_id`
- `case_id`
- `query_snapshot_json`
- `filters_json`
- `algorithm_version`
- `structured_weight`
- `semantic_weight`
- `created_by`

#### `case_comparisons`

- `comparison_id`
- `retrieval_run_id`
- `similar_case_id`
- `rank`
- `score`
- `similarities_json`
- `differences_json`
- `citation_locators_json`
- `adoption_status`

#### `report_versions`

- `report_version_id`
- `case_id`
- `report_type`
- `case_data_version`
- `plan_version`
- `rule_versions_json`
- `verification_status`
- `file_hash`
- `storage_uri`
- `exported_by`
- `watermark_id`
- `created_at`

#### `agent_runs`

- `agent_run_id`
- `case_id`
- `plan_item_id`
- `role`
- `model_name`
- `model_version`
- `prompt_template_version`
- `redacted_input_hash`
- `structured_output_json`
- `token_usage`
- `latency_ms`
- `cost`
- `retry_count`
- `status`
- `error_code`

#### `audit_events`

- `audit_event_id`
- `actor_id`
- `case_id`
- `event_type`
- `target_type`
- `target_id`
- `before_hash`
- `after_hash`
- `reason`
- `request_id`
- `ip_address`
- `device_info`
- `result`
- `created_at`

## 11. 数据不变量

1. 模型提取的每个事实至少绑定一个可反向定位的原文片段。
2. 原文定位至少包含文档版本、页码、文本块、字符区间和原文哈希。
3. 未找到信息只能保存为空和“未发现”，不能推测补全。
4. 人工修正不得覆盖历史值，必须产生新版本和复核记录。
5. 文档版本变化后，旧版本脱敏确认自动失效。
6. 已确认事实的上游片段变化后，事实至少变为 `PENDING_REVIEW` 或 `STALE`。
7. `STALE`、`INVALID` 或未确认关键事实不得用于正式报告和量刑计算。
8. 量刑只能使用用户明确选择且满足确认级别的事实。
9. 规则地区、行为时间或有效期不匹配时必须拒绝计算。
10. Plan 只能执行白名单动作，依赖未完成时不得执行下游项。
11. 自动重试不得突破步骤、模型调用和费用上限。
12. 解读问答为只读；修改请求必须创建 REPLAN。
13. 删除、替换材料必须先执行影响分析并由用户确认。
14. 模型输出必须通过 Schema、原文和安全校验，不能直接写入已确认业务表。
15. 每份正式导出物必须绑定案件数据、Plan、规则和报告版本。

## 12. AI 与确定性工具分工

### 12.1 DeepSeek V4 Flash 可执行

- 用户意图和请求边界分类；
- 生成及检查白名单 Plan；
- 文书类型辅助识别；
- 事实、材料评价和未确认信息分类；
- 帮信罪要素结构化提取与语义映射；
- 缺口自然语言说明；
- 已检索规范的适用条件解释；
- 类案相同点和不同点说明；
- 输出越界检查；
- 基于已确认成果的依据解读。

### 12.2 DeepSeek V4 Flash 不可执行

- OCR、PDF 版面解析和文件哈希；
- 作为脱敏唯一防线；
- 法律规则真实性、现行有效性的最终判断；
- 金额、日期、量刑幅度和统计指标计算；
- 向量检索；
- 证据能力、证明力、定罪和最终量刑判断。

### 12.3 强制控制

- 固定、版本化 JSON Schema；
- 法律依据只能从检索结果中选择；
- 输入不存在的金额、日期和主体不得输出；
- 每项事实绑定原文，反向匹配失败即无效；
- 低置信度、冲突和关键字段强制人工复核；
- 独立输出校核只读取原文、结构化输出和依据，不读取前一调用隐性推理；
- 模型升级前使用固定测试集回归。

## 13. 安全、隐私与审计

### 13.1 安全要求

- 原始材料、脱敏映射、结构化成果分区存储；
- 传输使用 TLS，静态数据使用机构密钥加密；
- 脱敏映射使用独立密钥和独立权限；
- 外部模型统一经服务端网关调用，客户端不得直连；
- 每次调用记录文档版本、脱敏确认版本和调用目的；
- 身份证号、完整手机号、详细地址、完整账号不得进入普通日志；
- 测试、开发、生产环境严格隔离；
- 生产案卷不得用于模型训练、提示词调试或无授权评测；
- 导出文件默认带导出人、时间和唯一编号水印；
- 查看脱敏映射、跨部门授权、原文导出等高风险操作需要二次确认；
- 模型服务未获授权或不可用时，系统保留本地解析和人工整理能力。

### 13.2 必须审计的事件

- 登录失败、权限拒绝和角色变更；
- 案件创建、归档和重新开启；
- 文件上传、替换、删除、下载；
- 脱敏项修改和外部调用确认；
- Plan 创建、确认、暂停、取消、恢复和重规划；
- 每次模型调用和确定性工具调用；
- 人工确认、修正、排除、暂不判断和重新开启；
- 法律规则发布、停用和回滚；
- 量刑情景创建和计算；
- 报告、原文和日志导出。

审计记录必须能回答：谁在何时基于哪个材料、Plan、规则和模型版本修改了什么，修改理由是什么，影响了哪些成果。

## 14. 异常与降级

### 14.1 文件异常

- 文件损坏：单文件失败，其他文件继续；
- 加密 PDF：提示提供可读取版本；
- 重复文件：提示忽略或明确作为新逻辑材料；
- OCR 部分失败：保留成功页面，失败页可单独重试。

### 14.2 脱敏异常

- 高风险信息未确认：阻断外部调用；
- 脱敏映射写入失败：本次操作回滚，不发送请求；
- 用户取消确认：保留本地成果，Plan 进入 `waiting_user`。

### 14.3 模型异常

- 超时、限流：退避后最多自动重试一次；
- Schema 不合法：确定性修复或重试一次；
- 仍不合法：转人工复核；
- 输出越界：丢弃输出、记录安全事件；
- 服务不可用：保存检查点，允许稍后恢复或本地降级。

### 14.4 规则和计算异常

- 无适用地区规则：拒绝计算；
- 规则版本冲突：`blocked` 并通知业务管理员；
- 参数来源冲突：要求人工选择，不自动取最大、平均或最新值；
- 计算异常：保存输入快照和失败步骤，不展示半成品结果。

### 14.5 并发与版本异常

- 编辑过期版本时提示版本冲突并展示最新差异；
- 新材料到达时先安全结束当前原子步骤，再影响分析；
- 同一案件只允许一个 Plan 修改有效成果；
- 用户可见错误包含简明原因、影响范围、已保存内容、下一步、错误编号和是否可重试。

## 15. 非功能需求

以下为 MVP 建议目标，评审后冻结：

- 普通列表和详情查询 P95 小于 2 秒；
- 单案 2,000 页以内支持后台处理并持续显示步骤状态；
- 单文件默认上限 500 MB，单案默认上限 20 GB，均可配置；
- 服务重启后从最近检查点恢复，不重复有效的已完成步骤；
- 相同输入、规则版本和参数的确定性计算结果完全一致；
- 页面状态不得使用虚假线性进度，应显示当前步骤、完成项和等待项；
- 关键操作可用性目标 99.5%；
- 支持键盘操作、明确焦点状态和高对比度风险提示；
- 所有耗时、Token、调用次数和费用可度量；
- 审计日志只追加、不可由普通业务用户修改。

## 16. 指标与发布门槛

### 16.1 核心指标

- 要素提取准确率、召回率和 F1；
- 数值字段准确率；
- 原文定位准确率；
- 要素映射准确率；
- 缺口召回率和误报率；
- 冲突识别率、反向材料遗漏率；
- 越界请求拦截率、正常请求误拦率；
- 无原文支持内容比例；
- 法条引用正确率、失效规范引用率；
- Plan 动作合法率、必做步骤遗漏率、恢复成功率；
- 增量影响范围识别准确率和全量重算一致性；
- 量刑计算正确率和可复现率；
- 类案 Top-3/Top-5 命中率；
- 单案处理时间、人工整理时间、人工修正次数和调用成本。

### 16.2 MVP 发布门槛

具体阈值由业务专家和测试集评审冻结。至少满足：

- 越界结论漏检率和失效规范引用率无阻塞级问题；
- 所有正式报告事实引用均可回链；
- 金额计算和规则计算在固定测试集上完全可复现；
- 增量更新不丢失未受影响的人工确认；
- 安全测试确认未脱敏材料不能外发；
- 核心主流程至少有一套合成案卷端到端自动化回归。

## 17. 待确认产品决策

1. 正式部署是比赛原型、内网试点还是生产系统；
2. DeepSeek 的部署位置、数据留存政策及机构授权条件；
3. 首版量刑规则支持的具体省份；
4. 一个案件支持多名涉案人员时，哪些要素按人评估、哪些按案评估；
5. 身份、核心行为、关键金额、量刑情节和冲突分别由哪个角色确认；
6. 单案页数、文件大小、复杂表格和手写材料的支持边界；
7. 缺口规则中法律要素、材料完整性和经验检查的责任维护人；
8. 类案数据授权、去重、更新频率和撤回机制；
9. 类案结构化权重和语义权重的维护与版本策略；
10. 量刑工具计算的是量刑起点、基准刑、调节幅度还是何种规范参考区间；
11. 原始材料、脱敏映射、模型日志、导出物和审计日志的保存期限；
12. P0 是否纳入完整关系图，或仅提供实体关系列表；
13. Streamlit 是否只用于比赛原型，何时触发正式架构迁移。

## 18. Issue 拆分原则

每个 Issue 必须是可独立演示的纵向切片，原则上同时包含：

- 一段明确的用户旅程；
- 页面或交互入口；
- 服务端业务逻辑；
- 数据持久化和版本规则；
- 权限、安全和审计；
- 自动化测试及合成演示数据。

不得将“只做全部页面”“只建全部数据库”“只封装全部模型接口”作为独立业务 Issue。基础设施工作应放入首个使用它的垂直切片，必要时再拆为不直接排期给业务验收的技术子任务。

所有 Issue 的默认完成定义：

- 通过代码评审、静态检查和自动化测试；
- 异常路径有用户可理解提示；
- 权限在服务端校验；
- 关键写操作有审计记录；
- 模型输出通过 Pydantic Schema；
- 事实结果包含原文定位；
- 不出现越界结论；
- 至少有一条合成案卷端到端验收用例。

## 19. 垂直切片 Issues

### 里程碑 M0：安全材料入口

### LA-001 创建并进入案件工作台

**用户价值**

承办人员可以创建一个独立、可恢复、受权限保护的案件任务。

**范围**

- 任务列表、创建表单、单案工作台骨架；
- 保存案件、涉案人员、成员权限和基础审计；
- 展示材料、计划、成果和报告导航；
- 支持归档和重新打开只读案件。

**依赖**

- 无。

**验收标准**

1. Given 有创建权限的用户填写必填字段，When 提交，Then 生成唯一案件 ID 并进入工作台。
2. Given 应用重启，When 用户重新登录，Then 可继续打开已创建案件。
3. Given 非案件成员，When 访问案件 URL，Then 服务端返回无权限且记录审计事件。
4. Given 用户归档案件，When 再次进入，Then 案件只读且历史数据未被物理删除。
5. 创建、查看、归档和重新开启均记录操作者、时间和结果。

### LA-002 上传、解析并定位案卷

**用户价值**

用户能确认系统读取了哪些材料、哪些页面失败，并能从解析文本回到原页。

**范围**

- PDF、DOCX、TXT、PNG/JPG 批量上传；
- 文件哈希、去重、文档版本、解析和 OCR；
- 材料列表、页级预览和解析失败重试；
- 文档、版本和片段持久化。

**依赖**

- LA-001。

**验收标准**

1. 上传混合格式案卷后，每份文件获得稳定文档 ID、版本、哈希和状态。
2. 扫描 PDF 可生成带页码和块位置的 OCR 片段。
3. 重复上传相同哈希文件时提示重复且不创建新版本。
4. 损坏文件失败不影响其他文件完成处理。
5. 任一解析片段可跳转到对应文档版本和页码。
6. 替换文件创建新版本，旧版本仍可审计查询。

### LA-003 本地脱敏并确认外部调用

**用户价值**

用户能逐项确认敏感信息不会未经许可发送到外部模型。

**范围**

- 本地识别姓名、身份证号、手机号、地址、账号和案件编号；
- 原文/脱敏结果对照、补充遮蔽和误遮蔽修正；
- 脱敏映射加密存储；
- 文档版本级外部调用确认。

**依赖**

- LA-002。

**验收标准**

1. 未完成高风险脱敏项确认时，任何外部模型调用均被服务端阻断。
2. 用户确认后，测试网关收到的载荷不含原始敏感值。
3. 用户可以增加遮蔽或撤销误遮蔽，操作有审计记录。
4. 文件替换后，旧版本脱敏确认自动失效。
5. 无查看映射权限的用户不能获取原始值。
6. 脱敏映射写入失败时，不发送模型请求。

### LA-004 请求安全门控与合规替代

**用户价值**

用户在提出越界请求时能得到明确限制和可继续完成的合规任务。

**范围**

- 请求分类为允许、需澄清、拒绝；
- 拦截定罪、证明标准评价和确定刑期请求；
- 推荐事实整理、材料缺口核对、规则条件说明等替代任务；
- 保存门控结果和规则版本。

**依赖**

- LA-003。

**验收标准**

1. “请判断是否构成帮信罪”被拒绝，并建议改为待证要素—证据整理。
2. “现有证据是否确实充分”被拒绝，且不调用完整案卷模型。
3. “整理支付结算金额及来源”可放行。
4. 含义不明确的请求进入 `waiting_user` 并给出具体澄清问题。
5. 请求文本、分类、规则版本和处理结果可审计。

### 里程碑 M1：事实—证据审查闭环

### LA-005 INITIAL_PLAN 计划确认与受控执行

**用户价值**

用户可在执行前了解系统将做什么，并在运行中看到每一步的真实状态。

**范围**

- 生成白名单动作 DAG；
- 计划确认页、强制/可选步骤和依赖展示；
- Plan、计划项、依赖和检查点持久化；
- 暂停、取消、失败重试和恢复；
- 步骤、模型调用和费用上限。

**依赖**

- LA-004。

**验收标准**

1. 白名单外动作被 Python 校验器拒绝。
2. 脱敏、证据绑定和输出校核等强制步骤不能关闭。
3. 依赖未完成时下游步骤不能启动。
4. 单项自动重试不超过一次，达到预算后停止并说明原因。
5. 暂停后恢复不会重复仍有效的已完成步骤。
6. 每个计划项可查看输入摘要、结构化输出、校验结果和错误，但不展示隐性推理。
7. 只有满足完成条件时 Plan 才能标记 `completed`。

### LA-006 提取可追溯事实、评价与实体

**用户价值**

检察官能快速查看材料中的具体事实、他人评价和系统无法确认的信息。

**范围**

- 提取帮信罪四类维度相关字段；
- 区分客观事实、材料评价和未确认信息；
- 提取人物、账户、设备、金额和时间；
- 要素卡初版、原文定位和低置信度队列；
- Schema、数值和原文反向校验。

**依赖**

- LA-005。

**验收标准**

1. 每项模型提取结果至少绑定一个可反向匹配的原文片段。
2. 未找到字段保存为“未发现”，不得推测补全。
3. “交易异常”保存为材料评价，具体时间和金额保存为客观事实。
4. 输入不存在的金额被模型输出时，校验失败且不写入有效事实。
5. 每个评价保存评价主体和评价来源。
6. 低置信度字段自动进入人工复核队列。

### LA-007 构建要素—事实—证据映射

**用户价值**

用户能查看每个帮信罪待证要素对应的材料、反向材料和原文。

**范围**

- 四类要素视图；
- 要素—事实—片段映射；
- 支持、反向、提及和评价来源分组；
- 映射状态和原文跳转；
- 依赖关系持久化。

**依赖**

- LA-006。

**验收标准**

1. 一个事实可绑定多个来源，且各来源可独立跳转。
2. 支持材料与反向材料分开展示。
3. 无来源事实不得标记为有效映射。
4. 删除映射不删除原始事实和材料。
5. 页面仅使用“已发现材料、未发现对应材料、存在冲突、待确认”等允许术语。
6. 每条映射可追溯到文档版本、页码、片段和要素模板版本。

### LA-008 人工复核与修正闭环

**用户价值**

检察官保留关键事实的确认权，且任何修改都可追溯和局部更新。

**范围**

- 统一复核队列；
- 确认、修正、排除、暂不判断；
- 修改理由、前后值、版本控制；
- 下游影响预览；
- 人工复核记录和审计。

**依赖**

- LA-006、LA-007。

**验收标准**

1. 修改关键金额前显示将受影响的映射、时间轴、缺口和量刑情节。
2. 用户取消修改时不产生业务数据变化。
3. 确认结果显示确认人、时间和版本。
4. “暂不判断”不被系统当作确认或否定。
5. 修改不会覆盖旧值，历史版本可查看。
6. 两人并发修改同一版本时，后提交者收到版本冲突和差异提示。

### LA-009 发现并处置材料缺口与冲突

**用户价值**

用户能集中核对客观缺失与不一致信息，而无需系统作出实体结论。

**范围**

- 缺口和冲突规则；
- 列表筛选、双侧原文对照和处置；
- 规则类别、优先级、状态和处置理由；
- 补充材料后的消除状态。

**依赖**

- LA-007、LA-008。

**验收标准**

1. 同一账户在两份材料中金额不一致时，生成可查看双方原文的冲突。
2. 文书引用未上传附件时，生成材料完整性缺口。
3. 仅有“交易异常”评价而无具体事实时，生成支撑缺口。
4. 输出只使用“材料未发现”“信息不一致”“待核验”等允许表述。
5. 用户关闭缺口或冲突时必须选择处置类型并填写理由。
6. 补充对应材料后记录可变为“已消除”，历史仍保留。

### LA-010 时间轴与实体关系视图

**用户价值**

用户能直观看到行为、资金和程序事件的顺序，以及人物—账户—设备关系。

**范围**

- 时间轴和关系图/列表；
- 时间精度、冲突标记和多来源；
- 实体合并、拆分和关系修正；
- 节点、边和事件原文回链。

**依赖**

- LA-007、LA-008。

**验收标准**

1. 事件按规范化日期排序，模糊日期明确显示精度。
2. 日期冲突并列展示，不自动选取正确值。
3. 拖动事件只改变显示顺序，修改日期需走事实修正流程。
4. 合并同一主体后，下游关系同步引用新实体版本。
5. 无来源的关系边不得标记为已确认。
6. 任一节点、边和事件可跳转对应原文。

### LA-011 新增材料影响分析与 REPLAN

**用户价值**

补充侦查材料后，用户只需复核真正变化的内容。

**范围**

- 新增、替换、删除文档；
- 片段差异、成果依赖图和影响预览；
- 新 Plan 版本、`stale` 标记和局部重算；
- 新旧版本差异和检查点恢复。

**依赖**

- LA-005 至 LA-010。

**验收标准**

1. 新增银行流水仅使关联账户、金额、资金时间轴、映射和相关情节失效。
2. 不受影响且已确认的身份事实保持有效。
3. 用户确认影响预览前不得开始重算。
4. 删除材料后，仅依赖该材料的事实不得继续有效。
5. 增量处理结果与相同版本全量重算的结构化结果一致。
6. 处理中断后可从最早受影响步骤恢复。
7. 版本差异明确显示新增、修改、删除、消除和新出现的冲突。

### 里程碑 M2：受控辅助与成果交付

### LA-012 量刑情节核验与可复算区间

**用户价值**

检察官能核验情节和规范计算过程，而不是接收黑盒确定刑期。

**范围**

- 主动开启、地区和行为时间选择；
- 已确认事实及情节选择；
- 规则版本、材料和适用条件核验；
- Python 计算、步骤展示、情景复算和版本保存。

**依赖**

- LA-008、LA-009；
- 已核验的首版地区法律规则数据。

**验收标准**

1. 未经用户主动开启不得自动计算。
2. 地区、有效期或行为时间不匹配时拒绝计算。
3. 每个参数可回链来源事实，人工输入明确标记。
4. 相同输入和规则版本重复计算结果完全一致。
5. 存在未处理关键冲突或 `stale` 输入时不输出区间。
6. 页面和导出物明确标注“规范化计算参考，非最终量刑建议”。
7. 每一步展示规则、公式、输入、中间值、舍入和输出。

### LA-013 相似案例检索与差异说明

**用户价值**

用户能找到来源可靠的候选类案并理解关键差异和数据局限。

**范围**

- 硬过滤、结构化相似度和语义相似度；
- Top 3—5 结果；
- 相同点、不同点、原文定位和采用状态；
- 检索算法、权重和数据集版本留痕。

**依赖**

- LA-008；
- 已授权、已结构化的类案数据集。

**验收标准**

1. 每个候选有案号、法院、裁判日期、来源、获取日期和原文定位。
2. 不满足硬过滤条件的案例不能进入结果。
3. 差异说明只能引用当前案件已确认成果和候选案例原文。
4. 不使用案例平均刑期推导当前案件刑期。
5. 用户可选择是否纳入报告，未采用结果不自动进入正式报告。
6. 检索结果显示类案库地区、年份和完整度限制。

### LA-014 输出校核与版本化报告

**用户价值**

用户能导出可追溯、可审计并经过安全检查的审查辅助成果。

**范围**

- 引用、规则版本、数值、反向材料和越界校核；
- 问题回退对应 Plan 项；
- Word/PDF 报告、Excel 映射表和日志导出；
- 报告版本、输入快照、水印和权限。

**依赖**

- LA-009；
- 可选包含 LA-012、LA-013 的结果。

**验收标准**

1. 存在无来源事实、失效法条、越界表述或 `stale` 关键成果时禁止正式导出。
2. 报告引用均可定位到文档版本和页码。
3. 报告区分系统提取、材料评价和人工确认。
4. 重复导出生成新版本，旧版本保持可审计。
5. 未运行量刑或类案时标记“本次未运行”，不生成虚假内容。
6. 正式报告带导出人、时间、唯一编号和版本信息。
7. 无正式导出权限的用户只能生成带醒目标记的草稿。

### LA-015 工作流后依据解读问答

**用户价值**

用户能追问结果为何形成，并直接回到依据。

**范围**

- `completed` 后开放；
- 基于已确认成果、原文和核验知识库回答；
- 引用跳转、无依据拒答；
- 修改类请求转 REPLAN。

**依赖**

- LA-014。

**验收标准**

1. `waiting_user`、`blocked`、`failed` 状态不能进入自由问答。
2. “为什么映射到主观明知相关事实”返回对应事实、原文和映射依据。
3. 无依据问题答复“当前材料中未发现”，不得补造。
4. 用户要求修改金额时不直接写库，而是展示影响并创建待确认 REPLAN。
5. 问答引用可跳转，回答和引用版本可审计。

### 里程碑 M3：管理、质量与试点增强

### LA-101 法律规则版本管理

**用户价值**

业务管理员可以安全发布和停用规范，不依赖研发改代码。

**依赖**

- LA-012。

**验收标准**

1. 未经专业复核的规则不能进入正式计算。
2. 新版本不覆盖旧版本，历史报告仍可复现。
3. 规则停用后，新计算不能使用，历史记录仍显示当时版本。
4. 发布规则前显示受影响地区、时间和待重新核验案件。

### LA-102 OCR 与复杂版面复核

**用户价值**

扫描质量较差或银行流水为表格时，用户能快速定位和修正识别错误。

**依赖**

- LA-002、LA-008、LA-011。

**验收标准**

1. 低质量页面自动进入 OCR 复核队列。
2. 原图与识别文本可对照修正。
3. 修正片段后只重算引用该片段的成果。
4. 表格金额支持行列一致性校验并标出异常行。

### LA-103 分析情景与版本比较

**用户价值**

用户可比较“采用/不采用某情节”的差异而不污染正式事实。

**依赖**

- LA-011、LA-012。

**验收标准**

1. 情景数据不覆盖正式案件成果。
2. 两个情景可逐项比较参数、规则和计算结果。
3. 采纳情景必须人工确认并产生新 Plan 版本。
4. 删除情景不影响正式成果和历史报告。

### LA-104 类案库覆盖看板

**用户价值**

用户能判断类案结果受到哪些样本范围限制。

**依赖**

- LA-013。

**验收标准**

1. 页面命名为“公开/授权样本分布”，不得称为犯罪分布。
2. 显示年份、地区、来源、完整度和关键要素缺失率。
3. 每项统计绑定数据集版本和计算时间。
4. 无数据时显示“暂无数据”，不得显示伪造比例。

### LA-105 系统质效看板

**用户价值**

项目人员可以按版本量化准确性、安全性、效率和成本。

**依赖**

- LA-001 至 LA-015 的运行日志和指标埋点。

**验收标准**

1. 指标可按系统、模型、规则和测试集版本筛选。
2. 每项指标显示定义、分子和分母。
3. 可查看匿名失败案例，不暴露真实身份信息。
4. 支持提取、映射、缺口、安全、增量、耗时和成本指标。

### LA-106 金标准与自动回归评测

**用户价值**

模型、提示词或规则升级后，团队能识别质量退化。

**依赖**

- LA-105。

**验收标准**

1. 隔离测试集不进入提示词调试或检索语料。
2. 支持单轮模型、普通 RAG、完整工作流三组对照。
3. 指标低于配置门槛时阻止版本标记为可发布。
4. 失败案例可回链匿名测试材料、预期标注和运行日志。

### LA-107 安全与部署加固

**用户价值**

系统具备进入受控试点环境的最小安全条件。

**依赖**

- 全部 P0/P1 Issue。

**验收标准**

1. 非案件成员无法读取材料、脱敏映射或报告。
2. 数据按留存策略清除正文、索引和缓存，并保留合规审计凭证。
3. 日志不含完整身份证号、手机号、账号或模型密钥。
4. 外部 API 失败时敏感内容不进入堆栈和普通日志。
5. 支持密钥轮换、导出水印和高风险操作告警。

## 20. Issue 依赖关系与推荐排期

### 20.1 最短可演示闭环

`LA-001 → LA-002 → LA-003 → LA-004 → LA-005 → LA-006 → LA-007 → LA-008 → LA-009 → LA-014`

该闭环可以演示：创建案件、上传案卷、脱敏、安全门控、受控执行、事实提取、证据映射、人工复核、缺口冲突和报告导出。

### 20.2 完整 MVP 关键路径

- 材料主线：`LA-001 → 002 → 003 → 004 → 005`
- 审查主线：`LA-005 → 006 → 007 → 008 → 009`
- 可视化主线：`LA-007 + LA-008 → LA-010`
- 增量主线：`LA-005...010 → LA-011`
- 量刑主线：`LA-008 + LA-009 + 规则数据 → LA-012`
- 类案主线：`LA-008 + 授权类案数据 → LA-013`
- 交付主线：`LA-009 (+ LA-012/013 可选) → LA-014 → LA-015`

### 20.3 可并行关系

- LA-010 可在 LA-009 开发期间并行，但共同依赖 LA-007、LA-008；
- LA-012 与 LA-013 可并行，且不应阻塞最短 P0 闭环；
- LA-101、LA-104 可分别在 LA-012、LA-013 稳定后并行；
- LA-102、LA-103 可在增量更新 LA-011 稳定后并行；
- LA-105 需要各功能提前完成统一日志和指标埋点；
- LA-107 的基础安全要求应随每个 Issue 交付，最终 Issue 仅完成试点级加固与验收。

### 20.4 五周建议

- 第 1 周：LA-001—LA-004，完成安全材料入口；
- 第 2 周：LA-005—LA-007，完成受控 Plan 和事实证据主链；
- 第 3 周：LA-008—LA-010，完成人工复核、缺口冲突和可视化；
- 第 4 周：LA-011、LA-014，完成增量更新和可交付报告；
- 第 5 周：LA-012、LA-013、LA-015 的演示版，以及回归测试、失败案例和答辩材料。

五周内不建议将生产级权限体系、500—1000 份高质量类案、150—200 份完整金标准和正式部署加固同时作为阻塞性交付。比赛版本应优先确保 P0 闭环真实可运行、可追溯、不过界。

## 21. 统一验收用合成案卷

建议使用一套贯穿全部 Issues 的合成案卷，至少包含：

- 起诉意见书；
- 证据目录；
- 两份存在部分陈述差异的讯问笔录；
- 银行流水摘要；
- 账户冻结材料；
- 电子数据摘要；
- 认罪认罚具结书；
- 退赃退赔材料；
- 一份被文书引用但首轮未上传的附件。

合成数据应预置：

- 一个明确事实与一项材料评价；
- 一处金额冲突；
- 一处日期冲突；
- 一项仅有结论性表述的缺口；
- 一项反向材料；
- 一份后补银行流水，可触发局部增量更新；
- 一项越界用户请求；
- 一组可复算的量刑演示参数；
- 3—5 个授权或合成类案候选。

该案卷必须显著标记为“合成测试材料”，不得包装为真实案件。
