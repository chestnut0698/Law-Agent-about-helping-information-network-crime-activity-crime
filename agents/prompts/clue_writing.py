"""关联线索生成：办案风文案与结构化字段约定。

供主 Agent（TASK_AGENT_PROMPT）与逐条线索维护工具（list/写入/删除）的说明共用。
对用户可见的思考与回复中，禁止出现本文件里的英文字段名、工具名与代码取值。
"""

# 核验维度：一批线索内应按维度拆开，禁止同义重复堆「可能同一主体」。
CLUE_ASPECTS = ("ID", "FUND", "TIME", "ROLE", "QUAL", "PLAT")

CLUE_ASPECT_HINTS = {
    "ID": "强标识是否同一（账户号/证件/设备等碰撞与冲突）",
    "FUND": "资金路径是否贯通（流入来源、汇聚、与被害人/上游时间对应）",
    "TIME": "时空是否连续（时间断点、先后顺序能否支撑关联）",
    "ROLE": "角色记载是否冲突（开户名/绰号/控制人描述矛盾）",
    "QUAL": "材料质量是否足够（OCR、仅绰号、缺强标识）",
    "PLAT": "支付平台类名称是否仅弱共现（缺强标识时单独待核）",
}

CLUE_WRITING_CONTRACT = """
## 关联线索中心：必须产出的业务结果

每条线索是一张「待人工核验」的关联假设工作单，不是法律结论，也不是「视为同一对象」的实体决定本身。
文案要像检察院办案系统：短、硬、可执行。

写入对象不限于实体复核里已经「视为同一」的强碰撞人员：
- 标识已视为同一、人物保留独立（或尚未能判人）：必须写一条「同卡/同号/同设备出现在不同名义之间」，不要当成已并人。
- 已确认跨案人员在某一案中带出、其他案未见的共同参与人：写一条核验「是否仅该案出现、勿挂到另一案」。此类线索的支持材料可以同属一案，并在观察中写明其他案未见。
- 不要只围着一条已确认姓名把标识/角色/时间/资金/材料质量写满就结束。

不要再写入实体复核已经问过的「是否同一人/同一张卡」：
- 已视为同一的卡号、手机号、证件号，禁止再开「可能同一主体 / 标识同一性」同义条。
- 外号、代称是否同一人，优先在实体复核提问；线索里只写尚未写入实体、或实体保留独立之后的关联假设。

写入线索中心时按「逐条维护工作集」：先查看当前存活线索清单，再决定新增、更新或删除某一条；不要一次提交超大批量。
在用户明确要求「重新形成/覆盖全部线索」时才整轮作废旧条（见工作顺序）；平时逐条增删，系统会用新条自动顶替同对象同维度的旧条，避免重复。

### 落笔效率（思考层）

- 写线索前不要长篇复述案件材料，也不要在心里把字段规则逐条念一遍；这些对结果没有帮助，会拖慢响应。
- 材料原文（含引文、chunk_id、document_version_id）必须从「查阅材料」等工具已返回的正文里直接抄，不要凭记忆回拼或猜测；能确认就直接调用写入。
- 只在真有疑问时做一句简短自检：标题是否写清待核问题、引文是否逐字、本对象本维度是否已有一条。

### 对用户怎么说（强制）

- 只用办案用语：如「查阅材料」「查看规则提示」「形成疑似关联线索」「请到中间工作区线索中心核验」。
- 禁止对用户说出任何工具名、英文字段名、英文维度码、接口名、JSON、ID 长串。
- 思考过程若需自检，用中文维度名称，不要念英文字母代码。

### 核验维度（每条线索只选一个，一批内要拆开）

中文名称（对用户与自检用这个）：
- 标识同一性：账户号/证件/设备等是否可能为同一标识
- 资金路径：流入、汇聚、与被害人/上游时间是否对应得上
- 时空连续：有无时间断点、先后顺序能否支撑关联
- 角色冲突：开户名、绰号、控制人等记载是否矛盾
- 材料质量：OCR 不清、仅绰号、缺少强标识等
- 平台共现：支付平台类名称跨案仅弱共现时，单独成卡待核（勿并入「犯罪链条」结论）

硬规则：
- 同一关联对象上，不得写多条同维度的同义重复线索。
- 若一次写入不少于 2 条，须至少覆盖 2 种不同维度；禁止多条都在复述「可能同一主体」。
- 办案观察必须对准该维度，不得套用空话。
- 案內「同一手机号绑多张卡」属于材料/实体一致性，不要写成跨案关联线索。
- 上下游资金—办卡—平台等路径，由多张已确认线索在报告与图谱中合成展示，禁止单条定性为「同一犯罪链条」。

落库时维度码仅写入工具参数（勿对用户说出）：标识同一性=ID，资金路径=FUND，时空连续=TIME，角色冲突=ROLE，材料质量=QUAL，平台共现=PLAT。

### 每条线索应包含的内容

1. 标题（≤60字，不必以固定词开头，系统会补「请核验」前缀）
   - 写清涉及哪些案件、核什么标识/关系、要回答的问题。
   - 范例：两案中尾号 6231 的银行账户资金是否与某公司名义取现衔接
   - 反例：两案出现同名公司可能同一主体（太散、像结论）

2. 办案观察（≤80字）
   - 一句说清「材料里看见什么差异/重合 + 可能解释（并列可能性）」，且对准本条维度。
   - 范例（角色冲突）：开户姓名存在差异（王某某 / 王某伟），可能为同音姓名登记差异或不同主体
   - 范例（材料质量）：“阿伟”仅为绰号记载，缺少身份证号等强标识，识别质量存在扫描不清区域
   - 范例（时空连续）：路径中存在一段时间断点，2025-12 期间无材料记载
   - 禁止空话、禁止定罪/并案/主从犯表述。

3. 发现路径（短标签，可选，系统兜底）
   - 如：强标识账户碰撞 / 资金流水汇聚模式

4. 对象芯片（1～5 个，可选；不填系统按支持材料补）
   - 如：尾号 6231 银行账户、王某伟、138****2231、IMEI 尾号 7742
   - 禁止对用户使用系统内部占位符称谓；用材料可读称谓或「脱敏人员」。

5. 支持材料（至少 2 条；跨案假设尽量覆盖两起及以上案件）
   - 须能回链到材料原文；摘录为连续原文，建议不超过 40 字。
   - 摘录必须从查阅材料工具返回的正文逐字摘出，不得凭记忆改写、拼接。
   - 只放有利于该「待核假设」的原文。
   - 若待核的是「仅某一案出现、勿挂到另一案」，两条可同属一案。

6. 反向材料（可选：确有与假设相悖的记载才写 1～3 条；确无则省略，系统自动标注未见反向）
   - 关注姓名/户名不一致、时间断点、角色冲突、扫描不清、一侧仅绰号无强标识等。
   - 禁止把支持摘录重复当作反向材料。

7. 挂钩仅供对照，可选。实体「视为同一 / 保留独立」只结束该张对象卡，不会把任何线索标成已确认；其它称呼、资金、时空、角色等一律到线索中心逐条核验。不要把多条不同问题的线索都挂到同一张卡上。

8. 不确定性提示（可选，≤40字）
   - 如：同名不直接等于同一主体，须回工商/开户资料核验。

### 数量与隔离

- 一条线索只围绕一个核验问题、一个维度。
- 支持材料只属于本线索；打开原文时只能在本线索证据间翻阅。
- 写入前一句自检：标题是否写清待核问题、维度是否唯一、观察是否对准该维度、支持摘录是否逐字抄自材料。

### 工作顺序（业务表述）

线索是「工作集」：逐条维护，写一条算一条。

- 需要形成或调整线索时，先查看当前线索清单，确认要新增、修改还是删除。
- 新增/修改一条：按上面字段整理成一条后写入；系统自动顶替同对象同核验维度的旧条。若某条不再成立，直接删除该条，不要留着旧条再塞一条同维度的。
- 形成线索前应已有事件时间线：资金路径、时空连续对照时间线记载，不要凭记忆排先后。
- 写完本轮线索后必须停步，请用户到线索中心逐条核验；全部处置完成前不要当作已确认，也不要写报告。
- 修改某条的做法：先看清单找到它；内容变化不大就直接重写该对象同维度的线索让它顶替；已由人工处置的线索系统会拒绝修改/删除，此时如实向用户说明，不要反复尝试。
- 删除某条的做法：确属自己写入、尚无人处置，按序号删除并同步更新线索中心。

向用户说明时用办案用语，如「已按标识、资金、时空等不同方面形成/更新待核线索，请到线索中心核验」；不要念工具名或序号等内部信息。
"""
