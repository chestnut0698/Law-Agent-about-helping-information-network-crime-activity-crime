---
description: 保持项目结构简洁并沿用现有命名风格
alwaysApply: true
---

# 项目文件结构规则

- 默认不新增文件。改动先落到已有文件里，只有在已有文件确实放不下、职责完全不同时才新建，并说明理由。
- 沿用 git 仓库原有的目录与命名：HTTP 接口写在 `app/main.py`，配置写在 `app/config.py`，工具与业务处理写在 `tools/` 下对应文件。
- 不新增分层目录（如 `api/`、`services/`、`repositories/`、`schemas/`、`parsing/`），保持扁平 Python 结构。
- 同一件事的全部环节合并在一个文件内，用注释分段，不拆成解析、存储、安全、编排等多个小文件。
- 一个文件对应一个明确业务功能，文件名直接体现职责，如 `files.py` 负责文件上传后的处理。
- 不要为目录例行创建空 `__init__.py`；仅在确实需要包初始化或公开导出时创建。
- 动手前先搜索现有实现，避免与已有代码重复；发现重复的解析、存储、检索或安全逻辑时先统一入口。
- 不为“未来可能需要”预建目录、抽象层、转发层或占位文件，只实现当前需求。
- 重构后同步修改全部 import、测试和 `docs/当前项目已有实际成果及其实现细节说明.md`。

# 文件更改规则
- 不要更改test/文件夹下的文件，功能验证我们走智能体正常全流程
- 默认接入 deepseek-v4-flash；NVIDIA 配置留给队友本地使用，不要删掉 test 里的 NVIDIA 探针
