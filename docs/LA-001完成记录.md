# LA-001 完成记录

## 依赖检查
- [x] 所有前置 Issue 已通过（LA-001 无前置 Issue）
- [x] D4：事实、要素和量刑情景支持按涉案人员维护，同时允许 `subject_id` 为空的案级共享数据
- [x] D5：检察官助理可初审，身份、核心行为、关键金额、量刑情节及关键冲突均由承办检察官最终确认

## 交付物
- 页面：`src/law_agent/app/main.py`、`pages/01_login_tasks.py`、`02_create_case.py`、`03_workbench.py`
- 服务：`auth_service.py`、`case_service.py`、`app/facade.py`
- 数据迁移：`alembic/versions/001_init_org_user_case.py`
- Schema：`src/law_agent/schemas/api/case.py`
- 配置：`src/law_agent/config/settings.py`、`.env.example`、`pyproject.toml`
- 测试：`tests/unit/test_rbac.py`、`test_case_service.py`、`test_case_schema.py`、`tests/integration/test_migration.py`、`tests/e2e/test_la_001_create_and_access.py`

## 验收结果
1. 有权限用户填写必填项后生成唯一案件 ID 并进入工作台：通过；证据：`test_create_case_is_unique_and_audited`、`test_la_001_create_restart_access_archive_and_reopen`、页面 `pages/02_create_case.py`
2. 应用重启后案件仍可查询：通过；证据：E2E 使用新 Session 再次 `get_case`
3. 非案件成员访问被服务端拒绝并记录审计：通过；证据：`test_nonmember_denial_is_persisted_in_audit`
4. 归档后案件只读，数据未物理删除：通过；证据：`test_archive_is_logical_and_reopen_requires_owner_prosecutor`
5. 重开仅承办检察官可执行：通过；证据：助理 `reopen_case` 抛出 `PermissionDeniedError`
6. 页面不直接使用数据库 Session：通过；证据：`test_streamlit_pages_do_not_open_database_sessions`

## 执行命令
- 命令：`python -m alembic upgrade head`；退出码：0
- 命令：`python scripts/seed_dev_users.py`；退出码：0
- 命令：`python -m pytest tests/unit/test_rbac.py tests/unit/test_case_service.py -v`；退出码：0（12 passed）
- 命令：`python -m pytest tests/e2e/test_la_001_create_and_access.py -v`；退出码：0（2 passed）
- 命令：`python -m pytest -v`；退出码：0（18 passed）
- 命令：`python -m ruff check src tests`；退出码：0

## 数据与安全
- 权限校验位置：`CaseService.assert_case_access`、`create_case` 角色检查
- 审计事件：`LOGIN_SUCCEEDED`、`LOGIN_FAILED`、`CASE_CREATED`、`CASE_VIEWED`、`CASE_ARCHIVED`、`CASE_REOPENED`、`PERMISSION_DENIED`
- 模型角色及 Prompt 版本：不适用（LA-001 无模型调用）
- 原文绑定：不适用（LA-001 无材料解析）

## 遗留问题
- D3、D7–D13 仍未冻结，不影响 LA-001，但会阻塞对应后续 Issue
- 本地 Streamlit 页面通过服务门面操作，未使用浏览器驱动做 UI 点击级 E2E
- 开发种子密码仅用于比赛原型，不能用于真实办案环境
