# 链证智析

 目前项目名称为：

“链证智析”——面向基层检察院的帮信罪关联案件证据链条挖掘与漏犯漏罪智能监督系统

## 需求

处理串联不同案件卷宗里的证据，发现共同点，发掘上下游犯罪链条，辅助发现遗漏线索。或者能够从大量证据，如涉及大量言词证据、鉴定意见、电子数据等多种证据（诈骗罪、强奸罪、故意伤害罪等）中提取事实和证据要素，重在**发挥大语言模型的语义理解能力**。

## 项目结构

```
仓库根/
D:.
│   .env
│   .env.example
│   .gitignore
│   README.md
│   requirements.txt
│
├───agents
│       base_agent.py
│       react_agent.py
│
├───app
│       config.py
│       files.py
│       main.py
│       tasks.py
│    
├───data
│   ├───database
│   └───storage
│
├───knowledge
│   ├───legal_interpretation
│   │       意见.docx
│   │
│   └───legal_library
│           法律.docx
│
├───tools
│       entities.py
│       get_time.py
│       legal_search.py
│       materials.py
│       policy_query.py
│       run_tasks.py
│       search_lawlibrary.py
│       tools.py
│       tools_schema.json
│       web.py
│    
│
└───ui
    │   index.html
    │
    ├───js
    │       agent.js
    │       file-upload.js
    │       input.js
    │       main.js
    │       markdown.js
    │       message.js
    │       plan.js
    │       sidebar.js
    │       state.js
    │       theme.js
    │       thinking.js
    │       toast.js
    │       tool-call.js
    │       utils.js
    │       workbench.js
    │
    └───styles
            chat.css
            input.css
            main.css
            markdown.css
            plan.css
            responsive.css
            sidebar.css
            thinking.css
            toast.css
            tool-call.css
            workbench.css
 
```

其中 `ui/` 存放前端，`knowledge/` 存放法律文件及相关知识库，`app/config.py` 存放提示词和流程，`tools/` 存放工具函数，`data/` 存放不同任务·数据，统一依赖`task_id`分开不同任务，实现多任务管理。

## 项目实现

**智能体 = 大语言模型 + 提示词 + 数据库 + 工具**

## 技术栈

后端语言/运行时：Python 3.x（代码面向 3.11+），前端为静态 HTML/JS。

Web 框架：FastAPI + uvicorn（SSE 流式响应）。

模型/LLM调用：openai 客户端。

数据持久化：SQLite（sqlite3），

文档处理/OCR：PyMuPDF(fitz)、python-docx、Pillow、PaddleOCR

脱敏/NLP：presidio_analyzer + spaCy (zh_core_web_trf)、正则匹配。

其它辅助库：python-dotenv、pydantic、requests、baidusearch、prikit、pytest。

## 注意事项

脱敏不完全准确，可能需要人工复核

### 已有

默认接入 DeepSeek 的 `deepseek-v4-flash`（读取 `DEEPSEEK_API_KEY`）。

当前对话按监督任务绑定：`/chat/{task_id}` 将消息写入 SQLite `chat_messages`。工作台主分析走 `POST /api/tasks/{task_id}/agent/chat`（DeepSeek ReAct + 任务工具）。会话附件兼容路径仍可用工作区目录；正式卷宗必须走材料管线。ReAct 智能体会调用模型；按项目提示词，智能体会自主调用工具完成对案件的分析。

